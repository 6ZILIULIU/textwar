import UnitBase from "./UnitBase";
import IUnit from "./IUnit";

const { ccclass, property } = cc._decorator;

@ccclass
export default class TankUnit extends UnitBase implements IUnit {

    start() {

    }

    // update (dt) {}
}
